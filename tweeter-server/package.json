{
  "name": "tweeter-server",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "tweeter-shared": "file:../tweeter-shared"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "rimraf": "^6.0.1",
    "typescript": "^5.0.2"
  }
}
